<div class="comment-block"> 
	<h4 class="text-capitalize lead"> <%= comment.author_name %> :
	<small class="lead"> <%= comment.body %> | <h5> <small> Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> ago</small></h5> </small>
	</h4>
</div>

